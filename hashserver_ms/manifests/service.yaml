apiVersion: v1
kind: Service
metadata:
  name: log-file-svc
  namespace: exercises
spec:
  type: ClusterIP
  selector:
    app: log-file
  ports:
    - port: 2346
      protocol: TCP
      targetPort: 3000