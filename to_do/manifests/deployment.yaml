apiVersion: apps/v1
kind: Deployment
metadata:
  name: to-do
spec:
  replicas: 1
  selector:
    matchLabels:
      app: to-do
  template:
    metadata:
      labels:
        app: to-do
    spec:
      containers:
        - name: to-do
          image: titunidoc/devops_kubernetes:1.2