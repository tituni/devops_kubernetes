apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: to-do-back-ingress
spec:
  rules:
  - http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: to-do-back-svc
            port:
              number: 2345