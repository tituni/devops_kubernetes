apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: to-do-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: to-do-svc
            port:
              number: 2345