apiVersion: v1
kind: Service
metadata:
  name: ping-pong-lb-svc
  namespace: exercises
spec:
  type: ClusterIP
  selector:
    app: postgres-pingpong-app
  ports:
    - port: 2349
      protocol: TCP
      targetPort: 3001