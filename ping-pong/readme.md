## Start-up of the service 

You can start manually the app in gke running this in the root folder of the the project:

```cmd
     kubectl apply -f manifests_google_gw
```

You can start manually the app locally running this in the root folder of the the project:

```cmd
     kubectl apply -f manifests
```
